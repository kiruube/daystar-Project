doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Update Record - Accounts.
    link(href='https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css', rel='stylesheet')
    link(rel='stylesheet', href='/css/style.css')
    style.
      .form-container {
        border: 2px solid #ccc;
        border-radius: 10px;
        background-color: #fff;
        padding: 20px;
      }
      h2{
        color: white;
      }
  include nav
  include buttonBar
  .container.mt-5
    h2.text-center.mb-4 Accounts Update
    .row.justify-content-center
      .col-md-6.form-container
        h4.text-center.mb-4 Update Record
        form#accountsform(action=`/accounts/update/${record._id}`, method='POST' onsubmit='return validation(event)' name='validationForm')
          .form-group
            label(for='recordType') Select Record Type:
            br
          .form-check.form-check-inline
            input#expenseRadio.form-check-input(type='radio' name='recordType' value='Expense' checked=record.recordType==='Expense')
            label.form-check-label(for='expense') Expense (Money Spent)
          .form-check.form-check-inline
            input#incomeRadio.form-check-input(type='radio', name='recordType' value='Income' checked=record.recordType==='Income')
            label.form-check-label(for='income') Income (Money Paid to Daycare)
          .form-group
            label(for='recordRefNo') Reference Number:
            input#recordRefNo.form-control(type='text' name='recordRefNo' value=record.recordRefNo)
          .form-group
            label(for='recordDate') Date:
            input#recordDate.form-control(type='date' name='recordDate' value=record.recordDate)
          .form-group
            label(for='recordDescription') Description:
            input#recordDescription.form-control(type='text' name='recordDescription' value=record.recordDescription)
          .form-group
            label(for='recordAmount') Amount (UGX):
            input#recordAmount.form-control(type='number' min='0' step='0.01' placeholder='Enter amount in UGX' name='recordAmount' value=record.recordAmount)
          button.btn.btn-primary.btn-block(type='submit') Update Record
  br
  include footer
  script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js')
  script(src="/js/acc.js") 
